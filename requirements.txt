# Core computer-vision / numerical stack
opencv-python>=4.8.0
numpy>=1.24.0
networkx>=3.0

# OCR engine. EasyOCR is the recommended default (pure-python install, GPU optional).
# pytesseract is supported as a fallback (requires the Tesseract binary installed on the system).
easyocr>=1.7.0
# pytesseract>=0.3.10

# Lecture vectorielle des P&ID PDF (detect_line_limits.py)
pymupdf>=1.23.0

# Surlignage : dépendance Excel
openpyxl>=3.1.0
